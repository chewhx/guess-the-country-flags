import React from 'react';

const header = (props) => {
    return (
        <section id="header">
            <div className="title"><h2>GUESS THE FLAG</h2></div>
        </section>

    )
};

export default header;